var React = require('react');

var App = React.createClass({
    render: function () {
        return ( <h1>
            is this thing on ?
            </h1>
        );
    }
});

module.exports = App;
